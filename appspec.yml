version: 0.0
os: linux
files:
  - source: /checker.html
    destination: /usr/share/nginx/html/
permissions:
  - object: /usr/share/nginx/html/checker.html
    pattern: "**"
    owner: ec2-user
    group: ec2-user
    mode: 444
